// export type ToastType = 'success' | 'error' | 'loading' | 'blank';

// const creatHandler = (type?: ToastType) => (message, options) => {
//     const toast = createToast(message, type, options);
//     // upsert(toast)
//     return;

// };