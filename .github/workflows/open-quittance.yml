name: deploy

on: [push]

env:
  DB_HOST: 0.0.0.0
  DB_PASSWORD: "password"
  DB_PORT: 3306
  DB_DATABASE: openQuittance
  DB_USER: root
  JWT_SECRET_TOKEN: 2y10s94DQTxK1txQ73nqiYAl4sFNJPnpjGpd0qUPN3pxaiAJg4Ucde6

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - uses: mirromutth/mysql-action@v1.1
        with:
          host port: 3306
          mysql version: "9.1"
          mysql database: "openQuittance" # Optional, default value is "test". The specified database which will be create
          mysql root password: "password"
          mysql user: "root"

      - name: Install Packages
        run: npm install

      - name: Build App
        run: npm run build

      - name: Build App
        run: rm -r node_modules && npm install --omit=dev

      - name: Checkout Build folder
        run: |
          dir

      - name: Install SSH Key
        uses: shimataro/ssh-key-action@v2
        with:
          key: ${{ secrets.SSH_KEY }}
          known_hosts: unnecessary

      - name: Adding Known Hosts
        run: ssh-keyscan -p ${{ secrets.PORT }} -H ${{ secrets.HOST }} >> ~/.ssh/known_hosts

      - name: c'est où que ça bloque
        run: |
          ssh ${{ secrets.USER }}@${{ secrets.HOST}}
          mkdir JEAN_la_SALLE

      - name: Deploy with rsync Build
<<<<<<< Updated upstream
        run: rsync -avz -e "ssh -p ${{ secrets.PORT }}" build ${{ secrets.USER }}@${{ secrets.HOST }}:/productions/open-quittance/build
=======
        run: rsync -avz -e "ssh -p ${{ secrets.PORT }}" build/ ${{ secrets.USER }}@${{ secrets.HOST }}:/etc/var/www/productions/open-quittance/build
>>>>>>> Stashed changes

      - name: Deploy NodeModule
        run: rsync -avz -e "ssh -p ${{ secrets.PORT }}" node_modules/ ${{ secrets.USER }}@${{ secrets.HOST }}:/productions/open-quittance/node_modules
